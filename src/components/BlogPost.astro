<article>
	<a href={`${Astro.props.slug}`}>
		<h2>{Astro.props.title}</h2>
		<p>{Astro.props.date}</p>
	</a>
</article>

<style>
a {
  /* font-size: 1.5rem; */
  color: var(--primary);
  text-decoration: none;
}

a:hover {
  color: var(--primary-hover);
}
</style>